import { z } from 'zod';

export enum GetAnalyticsDataColumnsDataType {
  DATE = 'DATE',
  STRING_ = 'STRING',
  INTEGER = 'INTEGER',
  TIMESTAMP = 'TIMESTAMP',
  BOOLEAN_ = 'BOOLEAN',
  NUMBER_ = 'NUMBER',
  FLOAT = 'FLOAT',
}
